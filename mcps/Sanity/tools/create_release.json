{
  "name": "create_release",
  "description": "Create a new release for grouping content changes. Releases are containers that hold versioned documents for coordinated publishing.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "intent": {
        "type": "string",
        "description": "Briefly explain why you are making this tool call — the high-level goal, not the specific operation. For example: \"migrating content from legacy CMS\", \"building a product catalog for launch\", \"cleaning up orphaned references after schema change\". Make sure to not include any secrets, API keys, or information that can be considered sensitive. This helps us understand usage patterns and improve the tools."
      },
      "resource": {
        "type": "object",
        "properties": {
          "projectId": {
            "type": "string",
            "pattern": "^[a-z0-9-]+$",
            "description": "The Sanity project ID"
          },
          "dataset": {
            "type": "string",
            "pattern": "^(~[a-z0-9]{1}[-\\w]{0,63}|[a-z0-9]{1}[-\\w]{0,63})$",
            "description": "The name of the dataset in the project"
          }
        },
        "required": [
          "projectId",
          "dataset"
        ],
        "additionalProperties": false,
        "description": "Resource information indicating which project ID and dataset to target"
      },
      "title": {
        "type": "string",
        "minLength": 1,
        "maxLength": 100,
        "description": "Human-readable title for the release (e.g. \"Q1 2024 Product Updates\")"
      },
      "description": {
        "type": "string",
        "description": "Optional description of the release"
      },
      "releaseType": {
        "type": "string",
        "enum": [
          "asap",
          "undecided",
          "scheduled"
        ],
        "default": "undecided",
        "description": "Release type: \"asap\" to publish immediately when ready, \"scheduled\" for a specific date, or \"undecided\" (default)"
      },
      "intendedPublishAt": {
        "type": "string",
        "format": "date-time",
        "description": "ISO 8601 date for when the release is intended to be published. Only relevant for \"scheduled\" release type."
      }
    },
    "required": [
      "resource",
      "title"
    ],
    "additionalProperties": false,
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}