{
  "name": "get_sanity_rules",
  "description": "Load Sanity development rules. ALWAYS call list_sanity_rules first to see available rules, then load all rules relevant to your task in a single call.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "intent": {
        "type": "string",
        "description": "Briefly explain why you are making this tool call — the high-level goal, not the specific operation. For example: \"migrating content from legacy CMS\", \"building a product catalog for launch\", \"cleaning up orphaned references after schema change\". Make sure to not include any secrets, API keys, or information that can be considered sensitive. This helps us understand usage patterns and improve the tools."
      },
      "rules": {
        "type": "array",
        "items": {
          "type": "string",
          "enum": [
            "angular",
            "app-sdk",
            "astro",
            "blueprints",
            "functions",
            "get-started",
            "groq",
            "hydrogen",
            "image",
            "localization",
            "migration-html-import",
            "migration",
            "nextjs",
            "nuxt",
            "page-builder",
            "portable-text",
            "project-structure",
            "remix",
            "schema",
            "seo",
            "studio-structure",
            "svelte",
            "typegen",
            "visual-editing"
          ]
        },
        "minItems": 1,
        "description": "One or more rule names to load. Use list_sanity_rules to see available rules."
      }
    },
    "required": [
      "rules"
    ],
    "additionalProperties": false,
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}